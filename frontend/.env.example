VITE_API_BASE_URL=http://localhost:8080/api
# Origin of the SEPARATE admin dashboard app (run with `npm run dev:admin`).
# The storefront's "Admin Console" link points here as a full-page navigation.
VITE_ADMIN_URL=http://localhost:5174/admin
