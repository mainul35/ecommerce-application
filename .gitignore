# Backend
backend/target/
backend/.mvn/
backend/HELP.md
# Machine-local Spring Boot overrides (e.g. non-default DB port).
# Both locations are covered because the IDE/CLI working directory may be
# either the repo root or backend/.
backend/config/
/config/
# Tesseract OCR traineddata for KYC — large binary models (~25MB), not source.
# Download per the README setup step; keep them out of git history.
backend/src/main/resources/test_data/
*.class
*.jar
*.war

# Runtime file storage — user uploads and private evidence. Never commit these.
# Created under the app working dir, which may be repo root or backend/.
uploads/
backend/uploads/
kyc-private/
backend/kyc-private/
dispute-private/
backend/dispute-private/
private-storage/
backend/private-storage/

# Frontend
frontend/node_modules/
frontend/dist/
frontend/dist-admin/
frontend/.vite/

# Env / secrets
.env
.env.local
.env.*.local
*.pem
*.key

# IDE
.idea/
.vscode/
*.iml
*.swp

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Claude metadata — commit the shared config, ignore personal overrides
.claude/settings.local.json
CLAUDE.local.md

# Stray Windows-reserved "nul" files left over by redirected commands
nul
**/nul
